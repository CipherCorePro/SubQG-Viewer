// This file is no longer used for direct Gemini SDK interaction
// as it has been moved to the backend server (`server/index.js`).
// Frontend calls the backend via `fetch` in `App.tsx`.

export {};
